export function initClipboard(){document.querySelectorAll("[data-copy]").forEach(b=>b.addEventListener("click",async()=>{const v=b.dataset.copyValue||document.querySelector("[data-contract-address]")?.textContent?.trim()||"";const old=b.textContent;try{await navigator.clipboard.writeText(v);b.textContent="COPIED";b.classList.add("is-copied")}catch{b.textContent="COPY FAILED"}setTimeout(()=>{b.textContent=old;b.classList.remove("is-copied")},1600)}))}