export function initReveal(){const a=document.querySelectorAll("[data-reveal]");if(matchMedia("(prefers-reduced-motion: reduce)").matches){a.forEach(x=>x.classList.add("is-visible"));return}const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("is-visible");o.unobserve(e.target)}}),{threshold:.12});a.forEach(x=>{x.classList.add("reveal");o.observe(x)})}