(()=>{const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];const header=$('[data-header]'),menu=$('[data-mobile-menu]'),toggle=$('[data-menu-toggle]'),close=$('[data-menu-close]');let lastFocus=null;const setMenu=open=>{if(!menu||!toggle)return;menu.classList.toggle('is-open',open);menu.setAttribute('aria-hidden',String(!open));toggle.setAttribute('aria-expanded',String(open));document.body.style.overflow=open?'hidden':'';if(open){lastFocus=document.activeElement;close?.focus()}else lastFocus?.focus?.()};toggle?.addEventListener('click',()=>setMenu(toggle.getAttribute('aria-expanded')!=='true'));close?.addEventListener('click',()=>setMenu(false));$$('[data-menu-link], [data-mobile-menu] a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&menu?.classList.contains('is-open'))setMenu(false)});const scrollHeader=()=>header?.classList.toggle('is-solid',scrollY>24);addEventListener('scroll',scrollHeader,{passive:true});scrollHeader();const io='IntersectionObserver'in window?new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -6%'}):null;$$('.reveal').forEach(e=>io?io.observe(e):e.classList.add('is-visible'));const coin=$('[data-coin]');coin?.addEventListener('click',()=>{const flipped=coin.classList.toggle('is-flipped');coin.setAttribute('aria-label',flipped?'Show the front of the $DayOne coin':'Flip the $DayOne coin')});const address=window.DAYONE_CONFIG?.token?.address||'',addressEl=$('[data-contract-address]'),copy=$('[data-copy]');if(address){addressEl.textContent=address;copy.disabled=false}copy?.addEventListener('click',async()=>{if(!address)return;try{await navigator.clipboard.writeText(address);const t=copy.textContent;copy.textContent='COPIED';setTimeout(()=>copy.textContent=t,1400)}catch{copy.textContent='COPY FAILED';setTimeout(()=>copy.textContent='COPY ADDRESS',1400)}});const forbidden=/\bDAYONE\b|\bdayone\b/;const walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT);let n,viol=[];while(n=walker.nextNode())if(forbidden.test(n.nodeValue))viol.push(n.nodeValue);if(viol.length)console.warn('$DayOne identity check: forbidden casing found.',viol)})();
